# VI sweep overlay: patch/fc = 0.5; softer ignore_small_comps while debugging.
# Stack: default_l40s + basic_long8h + this + eval_rm0
[vote_instances]
patch_threshold = 0.5
fc_threshold = 0.5
ignore_small_comps = 100
